# 指定运行程序py
FLASK_APP=app

# 指定ip(外部可见)
FLASK_RUN_HOST=0.0.0.0

# 指定端口
FLASK_RUN_PORT=8000

# 开发环境--调试模式会被开启：调试器-debugger和重载器-reloader(生产必须关闭) 默认是生产-production
FLASK_ENV=development

# 开发环境下想进行调试模式开关：1-开启(默认) 0-关闭(生产必须关闭)
# FLASK_DEBUG = 1
